#!/usr/bin/env ruby

project = ARGV.shift
branch = ARGV.shift || 'master'
script = "/home/#{project}/application/script/deploy"

if File.exists?(script)
  puts `sudo -u #{project} -i #{script} #{branch}`
else
  raise "ERROR: Make sure that there's a deploy script at #{script}"
end
