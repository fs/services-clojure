#!/bin/bash
old_path=$PWD
path=${1:-$old_path}
script=script/deploy

if [ -x $path/$script ]; then
  cd $path
  $script $2
  cd $old_path
else
  echo "ERROR: Make sure that there's a deploy script at $path/$script"
fi
